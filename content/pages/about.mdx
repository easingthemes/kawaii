---
title: About
blocks:
  - _template: hero
    headline: About
    subline: A second page, so the catch-all route has something to prerender.
  - _template: content
    body: |
      Any `.mdx` file added under `content/pages/` becomes a page at the matching
      URL. This one is at `/about`.
---
